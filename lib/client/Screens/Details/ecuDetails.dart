import 'package:flutter/material.dart';

class Ecudetails extends StatelessWidget {
  const Ecudetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Ecu Data'),);
  }
}